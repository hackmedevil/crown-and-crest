import type { ReactNode } from 'react'

interface ProductSlugLayoutProps {
  children: ReactNode
}

export default function ProductSlugLayout({ children }: ProductSlugLayoutProps) {
  return children;
}

export const metadata = {};
